server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: ${HOME}/Desktop/configs/
  application:
    name: config

health:
  config:
    enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
